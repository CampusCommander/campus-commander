# CC-12 implementation evidence

Date: 2026-09-08. Scope: bootstrap lifecycle and HTTPS edge foundation.

`deployment:bootstrap-test` passed an HTTPS test with synthetic certificate trust and a generated credential.
Missing and invalid credentials returned 401.
Valid credentials reached the startup page.
Worker, Kestra, traversal, and future application routes returned 404.
Frontend requests contained no bootstrap authorization header.
Responses did not contain the synthetic credential marker.

The PostgreSQL integration fixture passed persistent bootstrap lifecycle checks.
Those checks covered initialization, resume, expiry, replacement, stale-generation rejection, and revocation.
See [CC-7 evidence](CC-7.md) for the database fixture and exact commands.

The edge runtime resolves declared certificates and secret references.
Liveness remains separate from readiness.
The runtime aggregates migration, database, Redis, HTTP service, artifact, and authenticated Kestra checks.
The complete aggregator still requires an integrated profile test.

The district hostname, browser-trusted certificate, complete profile readiness, and browser walkthrough remain `not-run`.
The fixture certificate establishes local TLS behavior only.
CC-12 remains incomplete until integrated runtime and browser checks pass.

## Browser validation

Chromium 149.0.7827.55 passed six checks against the frontend development server.
The checks covered startup availability, readiness downgrade, empty-report rejection, stale observations, and both themes at 320 CSS pixels.
The page refreshes protected checks every ten seconds.
A failed refresh preserves previous checks and labels them stale.
The page shows observation age and exposes the exact timestamp through the time element.

Commands:

```sh
npm exec -- nx run-many -t build,test,lint -p frontend
PLAYWRIGHT_BROWSERS_PATH=/tmp/cc-phase1-browsers npm exec nx run frontend-e2e:e2e -- --args="--project=chromium"
```

These checks support UI-01, UI-03, UI-06, UI-08, UI-09, and UI-10.
The checks do not establish complete accessibility conformance or district browser trust.
The browser fixture intercepts startup responses. Full profile readiness remains a separate integration gate.
