# CC-16 Installer evidence

Date: 2026-09-08. Implementation: `deployment/installer/cli.mjs` and `orchestrator.mjs`.
Operator instructions and example reside in the same directory.

| Acceptance criterion                                                             | Available evidence                                                                                                                             | Remaining gate                                                            |
| -------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| Runtime, architecture, ports, certificates, storage, resources, time, and egress | Preflight records actionable checks. Actual fixture passed Docker, architecture, certificate, memory, registry, and port checks                | District capacity, DNS, time-service, and external endpoint qualification |
| Credential-safe errors                                                           | Fixed typed CLI errors and support allowlists. Tests reject injected credential-bearing errors                                                 | District support walkthrough                                              |
| Prebuilt-image installation                                                      | Actual CLI starts a dedicated all-Docker project from registry digest references without image builds                                          | Clean district Linux host and signed accepted release                     |
| Every profile's commands                                                         | Shared CLI dispatches all-Docker, hybrid primary/worker fragments, and Kubernetes renderers with explicit operator settings                    | Actual hybrid and district Kubernetes installer walkthroughs              |
| Resume and secret preservation                                                   | Injected interruption test and actual prepare/resume preserve credentials. Changed inputs reject                                               | District interruption walkthrough                                         |
| Restart data preservation                                                        | Actual stop/resume and uninstall/resume preserve artifact identity, SHA-256, and migration ledger                                              | Distributed filesystem restart qualification                              |
| Fixture upgrade                                                                  | Actual release A to different release B image digests preserves artifact, ledger, and bootstrap credential after encrypted backup verification | Accepted signed release upgrade and district recovery objectives          |
| Stop and uninstall preserve data                                                 | Actual CLI stop and uninstall retain state and volumes                                                                                         | District ownership review                                                 |
| Explicit erasure                                                                 | Exact owned project confirmation required. Actual erase leaves no project-labeled volumes                                                      | District external-storage ownership walkthrough                           |
| Kubernetes route                                                                 | CLI uses explicit context, required Secret checks, and deterministic manifests without a GUI                                                   | Qualified cluster execution of the installer                              |
| Protected startup checks                                                         | Actual CLI requires protected HTTPS startup and eight ready checks before success                                                              | District hostname and certificate deployment                              |
| Phase 1 independence                                                             | Fixtures use only infrastructure, artifact bytes, and bootstrap access                                                                         | No application login or business logic prerequisite introduced            |

## Commands and actual result

```sh
npm exec nx run deployment:installer-test
npm exec nx run deployment:installer-integration
npm exec nx run deployment:lint
```

Nine installer unit tests, five qualification tests, and final deployment lint passed.
The real CLI integration passed in 2 minutes 3 seconds.
It created one unique `cc-installer-` project and used loopback HTTPS with a verified synthetic hostname certificate.
It ran prepare, resume, stop, resume, uninstall, resume, upgrade, and explicit erase.
The fixture preserved one Unicode artifact, its opaque identity and SHA-256, and the migration ledger.
It preserved the active bootstrap credential byte-for-byte.
A real encrypted backup covered both PostgreSQL databases and both durable file trees before upgrade.
The installer authenticated every backup component and checked the current release image inventory before accepting upgrade.
Release B changed the frontend, API, and worker image digests.
The final CLI erase removed the fixture's owned containers and volumes. Temporary backup proxies were removed afterward.
The existing CC-13 source project and registry were not modified by this fixture.

Machine evidence is `CC-16-installer-result.json`.
It records the uncommitted candidate working tree and base Git revision separately.
The base Git revision does not claim committed provenance for these candidate images.
Release A used frontend `0dec92e116af875e794a6c77f9f6d060bab7312503ecde9bd3dbd20b3e5adc37`, API `3808d66d5ed973993f083bb3139429c9f9b9c446394f2f12503f31416e52993e`, and worker `f5fe051fa54730425074319b6512c70f0a1e7f86a890594c89ca46bf4fc60f26` digests.
Release B's complete repository references appear in the machine evidence.

The fixture used explicit qualification exceptions for synthetic DNS, district storage reservation, and host time-service evidence.
It inspected immutable image manifests through the explicit localhost HTTP registry fixture setting.
It retained service TLS verification and did not claim accepted-release status.
Normal installation rejects these exceptions and requires signed matching evidence for every profile.

## Failure and recovery checks

Tests cover injected command interruption, redacted errors, preserved credentials, changed-input rejection, and exact erasure ownership.
They reject unsigned releases and cryptographically signed releases that lack matching acceptance evidence.
The Cosign path requires exact trusted identity and issuer arguments without insecure verification flags.
Restore markers block startup while allowing diagnosis and stop commands.
Bootstrap recovery retains a synchronized pending credential before its database generation change.

The real fixture exposed Docker Desktop bind-mount failure after replacing unchanged configuration inodes.
The installer now preserves semantically unchanged runtime JSON files.
A regression test checks inode preservation through resume.
Upgrade explicitly recreates containers after image changes.

Actual accepted-release Cosign verification remains unrun because signed complete profile evidence is unavailable.
Interactive terminal walkthroughs, hybrid installation, district Kubernetes installation, and distributed worker-host checks remain unrun.
The same-host Docker fixture does not establish district shared-storage behavior.
CC-16 and the Phase 2 entry gate remain incomplete until all required profile acceptance checks pass.

The final review added generated manifest hashes and project-scoped volume/PVC inventories to installer state.
Tampering with an explicit volume name now blocks erasure before any Docker command.
Tests bind separate worker fragments and reject Kubernetes resources outside the owned namespace.
Pending manifest hashes permit only committed or planned content during interrupted rendering.

The real CLI lifecycle and image upgrade passed again after manifest binding.
The final run also verified explicit erasure and removed the owned target network after proxy cleanup.
