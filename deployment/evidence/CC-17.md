# CC-17 Foundation restore evidence

Date: 2026-09-08. Implementation: `deployment/operations/index.mjs` and `cli.mjs`.
The operator procedure and example reside in `deployment/operations/README.md` and `operator.example.json`.

| Acceptance criterion                             | Available evidence                                                                                                                  | Remaining gate                                                         |
| ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| Every durable component and consistent procedure | Both databases, both file trees, configuration, release inventory, protected secret references, and Redis discard policy documented | District cold-backup operator walkthrough                              |
| Separate backup material                         | Disjoint path checks reject primary-tree overlap                                                                                    | District confirmation of independent backing storage and mount aliases |
| Protected recovery keys                          | Separate 32-byte key, AES-GCM components, authenticated manifest, key-availability checks                                           | District escrow and retrieval procedure                                |
| Isolated restore per profile                     | Disposable PostgreSQL and file restore uses all-Docker configuration                                                                | Hybrid and Kubernetes restore walkthroughs                             |
| Restored integrity and state                     | Both database inventories, synthetic Kestra state, internal JSON, opaque artifact identity, Unicode bytes, and SHA-256 checks pass  | District profile recovery walkthroughs                                 |
| Measured time and recovery limits                | Fixture backup 261 ms and restore 213 ms. Recovery point is recorded writer stop time                                               | District dataset timing and declared recovery objectives               |
| Missing material fails                           | Missing/wrong key, missing source volume, missing encrypted component, and corrupt ciphertext rejected                              | District storage-provider failure verification                         |

Commands:

```sh
npm exec nx run deployment:operations-test
npm exec nx run deployment:operations-integration
```

Two unit tests and deployment lint passed after final formatting. The real integration fixture used PostgreSQL 18.6 with the CC-7 image digest.
The fixture created unique databases, roles, a Docker volume, and temporary directories. It removed all owned resources afterward.
Actual `pg_dump` and `pg_restore` ran inside that pinned image through the injected tooling seam.
The native operator-host tooling path was not run because host PostgreSQL backup binaries are absent.

The fixture produced six encrypted components without plaintext database dumps in the backup directory.
It rejected a live database connection before backup.
It restored both database dumps into a different empty database pair.
It preserved the published artifact identity and Unicode bytes through an adapter read.
It preserved synthetic Kestra execution state and a separate internal-storage fixture.
It rejected absent and incorrect keys, corrupt ciphertext, and a removed Kestra dump.
It rejected a missing source storage directory and backup placement inside primary storage.
It rejected a second restore into nonempty target databases.
An injected restore failure retained `RESTORE_DISABLED` and produced no success report.
Successful restore retained that marker and reported `verified-services-disabled`.

The small protocol fixture does not run the actual Kestra server or start a recovered installation.
It verifies the database-and-files recovery protocol with synthetic state.
The profile fixture below supplies fresh Redis verification for all-Docker. The library restore command does not start Redis.
Hybrid shared-storage and Kubernetes RWX restores were not run because qualified district environments were not supplied.
The measured durations describe this small local fixture. They are not district support guarantees.
CC-17 remains incomplete until complete profile restores and operator acceptance pass.

## Actual all-Docker profile restore

`deployment/operations/profile-integration.mjs` passed through `deployment:operations-profile-integration` after the source owner released its fixture.
The retained source project was `cc-fault-cc13`.
The fixture stopped API, workers, and Kestra before copying their real volume trees and dumping both databases.
Temporary loopback proxies provided operator connections to the isolated Docker databases.
The source database contents and volumes were not overwritten.

The fixture encrypted seven components. Backup took 378 ms. Offline restore took 358 ms.
The complete qualification task took 44.7 seconds, including target creation, startup, checks, and cleanup.
The new target used the same image digests, separate databases, separate named volumes, and a random loopback HTTPS port.
It verified the restored migration ledger, artifact identity and SHA-256, actual Kestra flow, and Kestra internal namespace file.
The protected HTTPS startup endpoint reported all eight checks ready.
A source Redis cache sentinel was absent from the fresh target Redis instance with tmpfs storage.
The fixture retained the configured least-privilege Redis ACL. It did not request unrestricted key enumeration.

Offline integrity acceptance explicitly released `RESTORE_DISABLED` before the qualification startup.
This controlled fixture does not establish automatic marker enforcement by every installation entrypoint.
The target containers, proxy containers, networks, and named volumes were removed after qualification.
The source writers remain stopped for the parent operator's next action. The source registry remains available.

Machine evidence is `CC-17-profile-result.json`.
It identifies the base Git revision and the uncommitted candidate working tree separately.
The recorded Git revision does not claim committed provenance for the tested candidate images.
The source fixture profile contained placeholder image references. The operator snapshot used the actual retained release image digests.
No source configuration file changed.

Hybrid and Kubernetes district restores remain untested. This same-host Docker result does not qualify district shared storage.
