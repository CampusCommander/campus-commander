# CC-19 release publication evidence

Status on 2026-09-09: signed candidate publication and rejection gates passed.

The [candidate workflow](https://github.com/CampusCommander/campus-commander/actions/runs/34359354716) passed validation, image publication, capacity checks, and bundle publication.
The [published candidate](https://github.com/CampusCommander/campus-commander/releases/tag/phase-1-candidate-3e6a6a67a2c3) contains its archive, manifest, and two signature bundles.
The manifest records source revision, image digests, Linux amd64 architecture, dependencies, checksums, SBOM inventory, and upstream provenance.
Anonymous release and image verification passed after GHCR packages became public.

The [hosted validation record](../installer/HOSTED-VALIDATION.md) covers actual installation and resume across all three profiles.
The release remains a candidate. Missing acceptance evidence blocks normal promotion.
Publication does not assert that every profile, restore, fault, or human acceptance gate passed on this revision.
Nx Cloud remains optional.

The closeout CI configuration adds operations and qualification tests to the main regression job.
It also runs the operations integration target in the foundation matrix.

## Closeout evidence binding

Each accepted check now identifies a unique report path and its exact inventory checksum.
The verifier checks the JSON report status, profile, check, source revision, and application images.
Both Ed25519 and Cosign verification paths require those checks.
Explicit candidate qualification remains unchanged.
Regression tests reject unrelated file hashes and mismatched report claims.
Four release tests and the complete installer suite passed.

## Historical assembly and workflow checks

The following entries describe earlier revisions. They do not override the publication result above.

Date: 2026-09-08.

The CI workflow now runs foundation tests and isolated database, Redis, and storage integration targets.
Nx Cloud remains optional.
These workflow edits have not run on GitHub Actions.

`deployment:release-test` passed local integrity rejection tests.
The tests reject modified manifests, modified file bytes, symlink substitutions, and missing profile acceptance.
All signing keys and evidence in that test are disposable synthetic fixtures.

Project-owned image publication, release signatures, SBOM, installation bundles, full profile gates, and GitHub CI results remain incomplete.
This evidence does not authorize a Phase 1 completion claim.

The candidate workflow now defines GHCR image publication, Cosign OIDC signatures, SPDX SBOMs, and signed installation bundles.
Local candidate assembly tests passed source binding, untracked-secret exclusion, file integrity, and production-gate rejection.
The workflow still requires an actual GitHub Actions run.
Candidate publication does not replace the outstanding profile acceptance records.

The bundle now includes locked PostgreSQL and schema-validation runtime dependencies.
An isolated temporary-directory check imported the installer successfully without workspace dependencies.
CI repeats that check after archive extraction and verifies every inventoried file before signing.
The installer also rejects generated deployment manifests that differ from its saved ownership state.
Nine installer tests and the repeated actual lifecycle/upgrade fixture passed with this protection enabled.

## Candidate capacity gate

The candidate capacity job now downloads the frontend, API, and worker publication artifacts.
It builds an explicit immutable image inventory from their recorded references.
It pulls those images and the renderer's pinned PostgreSQL, Kestra, and Redis images.
Every image pull uses Node `execFile` with a structured argument array.

The job retains the bounded artifact adapter check.
It also runs `deployment:profile-capacity-integration` with the candidate inventory.
The uploaded capacity artifact contains both machine result files.
The bundle job retains its existing dependency on the capacity job.

Actionlint 1.7.12 accepted the workflow on 2026-09-08.
Its archive matched published SHA-256 `8aca8db96f1b94770f1b0d72b6dddcb1ebb8123cb3712530b08cc387b349a3d8`.
All three inline Node programs passed syntax checks.
A controlled stub run verified the inventory and structured command arguments without pulling or publishing images.
GitHub Actions has not run this workflow revision.
No image, bundle, or release publication occurred during this validation.

## Committed bundle isolation check

The bundle check passed against commit `7b218fecae27619ff0b06e3c7d6f7b1847b501ce` in a clean detached checkout.
It installed locked runtime dependencies, assembled an archive, and extracted it into a separate directory.
The installer and qualification image helper imported from that extracted bundle.
Integrity verification covered 1,060 inventoried files, including the PostgreSQL secret parser and bundled dependencies.
The normal promotion gate rejected the missing profile acceptance evidence.
The check removed its temporary checkout and files.

[The result](CC-19-final-bundle-result.json) records the exact source revision and validation boundary.
Provenance inputs were explicit synthetic placeholders for this assembly test.
This check does not establish signatures, an SBOM, publication, or accepted release status.
