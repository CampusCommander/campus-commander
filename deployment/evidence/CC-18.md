# CC-18 evidence

Date: 2026-09-09.

Six local qualification tests passed through `deployment:qualification-test` before the complete-profile capacity harness was added.
They covered project and volume boundaries, restart after diagnostic failure, and real TLS certificate faults.
The TLS probe accepted a current trusted certificate.
It rejected expired and wrong-host certificates under that trusted CA.

The complete process harness is implemented in `deployment/qualification/faults.mjs`.
Its all-Docker execution passed seven interruptions and fourteen durable fixture checks.
Distributed dependency and near-full storage checks for other profiles remain incomplete.
The all-Docker edge certificate qualification passed as recorded below.
Artifact access loss passed in the all-Docker process fixture.
[CC-15 workload evidence](CC-15-workload-result.json) records synthetic Kubernetes worker rescheduling and artifact preservation.
That Kind result does not qualify district shared-storage loss or recovery.
Component storage tests provide separate interruption evidence under CC-9 and CC-10.
They do not replace this ticket's complete installation matrix.

## Complete all-Docker component run

[The result](CC-18-process-result.json) records process interruptions for API, workers, Redis, both PostgreSQL services, and Kestra.
It also records artifact access loss through the dedicated volume permissions.
Every interrupted component stopped reporting readiness and recovered within the configured 180-second deadline.
The measured interruption and recovery intervals ranged from 967 ms to 13,657 ms.
Fourteen checks preserved migration and bootstrap rows, artifact bytes, the Kestra flow, and its internal marker file.

The fixture used Docker Engine 29.7.2 and Compose 5.5.0 on one host.
It used a private synthetic certificate and local registry digests.
The code came from the uncommitted implementation worktree.
The renderer had not applied configured resource limits during this run.
Resource observations in the result therefore describe unrestricted synthetic containers.
The resource-limited repeat below supersedes this unrestricted run.
Final published-image qualification remains outstanding.

## Bounded capacity fixture

[The capacity result](CC-18-capacity-result.json) records an eight-megabyte temporary artifact filesystem and isolated PostgreSQL database.
The fixture exhausted that filesystem and observed storage readiness fail.
A new artifact upload failed without creating another ready publication.
Removing the filler restored storage readiness and preserved the original artifact checksum.
The fixture cannot fill the host disk and creates no district capacity guarantee.
This adapter check does not replace a near-full test against every complete profile.

## Complete all-Docker capacity fault

[The complete-profile capacity result](CC-18-profile-capacity-result.json) records a bounded all-Docker ENOSPC check.
The 34.6-second harness started all eight components with the final local application images.
The Nx target completed in 39.7 seconds.
It observed eight ready protected startup checks before the fault and after recovery.

The fixture replaced only the rendered artifact volume with a 16 MiB Docker local tmpfs volume.
It preserved the rendered artifact mounts for volume permissions, API, and workers.
It verified the Linux tmpfs filesystem type and 16 MiB capacity before writing filler data.
The write consumed 16,769,024 bytes and reduced available capacity to zero.

Under ENOSPC, only the named `artifacts` startup check reported `not-ready`.
The attempted publication remained `staging` and inactive.
The ready artifact count stayed at one.
After filler removal, all eight startup checks recovered in 2,634 milliseconds.
The original 40-byte artifact retained SHA-256 `d58f626efe1927d08325499ae7ae6913aeacb7934a37adf5c1cc15784904e335`.

The run used these final local application images:

- Frontend: `localhost:15000/campus-commander/frontend@sha256:9ae5b788f8c21da072d1cf1b6cd506c0ed3abd1b5aab4d7e7110f97e144e5842`
- API: `localhost:15000/campus-commander/api@sha256:73cd46ece723ff04d0369ad755c202debb1b07e59527e65a25abc53c1ae3f6f5`
- Worker: `localhost:15000/campus-commander/worker@sha256:6e692bc185bcadb36ea1378aa5a321faa98c8fbf760058c5695dbad528688f0a`

The harness verified its unique resource labels before cleanup.
It then verified the removal of its containers, networks, and six volumes.
It did not stop the shared registry or touch existing Campus Commander volumes.

This synthetic tmpfs check makes no persistence claim.
It ran on one Docker host.
It does not reproduce ext4, quota, NFS, or object-storage behavior.
It qualifies the all-Docker artifact capacity fault only.

## Resource-limited repeat

[The repeated result](CC-18-limited-result.json) supersedes the unrestricted run for local component interruption evidence.
The renderer applied configured CPU and memory limits and normalized runtime image references.
All seven interruptions and fourteen fixture checks passed again through `deployment:qualification-integration`.
The command completed in 73 seconds.
The result records actual image references, resource measurements, the base Git revision, and the uncommitted candidate boundary.
Distributed profiles and committed release qualification remain separate outstanding gates.

## Complete all-Docker edge certificate faults

`deployment:installer-tls-integration` passed the complete installer lifecycle and both edge certificate faults in 183 seconds.
The fixture used the final local frontend, API, and worker image digests recorded in the machine result.
It established eight ready startup checks with a current certificate from its synthetic trusted CA.
The expired certificate produced `CERT_HAS_EXPIRED`. The wrong-host certificate produced `ERR_TLS_CERT_ALTNAME_INVALID`.
The protected HTTPS startup probe did not report readiness under either invalid certificate.
Each recovery returned eight ready checks and preserved artifact identity, artifact checksum, and the migration ledger.
Expired-certificate fault and recovery took 14,291 milliseconds. Wrong-host fault and recovery took 12,533 milliseconds.
The harness restored the original certificate and private-key bytes in its final cleanup block.
The installer then explicitly erased only its isolated project resources.

[The machine result](CC-18-edge-tls-result.json) records exact images, source state, TLS errors, and recovery measurements.
The result qualifies these two all-Docker edge faults only. It does not establish distributed-profile or complete fault-matrix acceptance.

## Complete hybrid storage and database certificate faults

[The hybrid result](CC-18-hybrid-storage-certificate-result.json) records three fault cells against one complete same-host hybrid fixture.
The fixture ran the published candidate images with one controller and two worker Compose projects.
All six artifact consumers shared one 16 MiB Docker tmpfs volume.
The run established complete startup readiness before fault injection.

The artifact access fault removed permissions from the shared artifact directory.
The named `artifacts` check reported `not-ready`.
Restoring permissions recovered readiness and preserved every original fixture.

The capacity fault wrote 16,769,024 bytes and reduced available space to zero.
The API rejected a one-megabyte publication without creating an authoritative artifact.
The ready artifact count remained one.
The harness removed the failed attempt and filler before recovery.

The certificate fault installed a matching certificate and private key from the trusted synthetic CA.
PostgreSQL completed a TLS handshake for `wrong-postgres.invalid`.
The district hostname produced `ERR_TLS_CERT_ALTNAME_INVALID`.
Both database checks reported `not-ready` until the harness restored the original key pair.

Every cell preserved the artifact, migration ledger, metadata, worker markers, Kestra execution, and Kestra file tree.
The result also records five process interruption cells and their recovery measurements.
It retains public image references, timestamps, checksums, readiness, resource measurements, and cleanup booleans.
The raw run did not record Docker Engine or Compose versions.
A read-only post-run check observed Docker Engine 29.7.2 and Compose 5.5.0 on the same daemon.
That observation does not establish the versions during the matrix.

The 111.9-second run removed every owned container, network, and volume.
An independent inventory check found no resource with its unique run prefix.
The sanitized result contains no runtime identifier, private path, project name, network name, or volume name.

The published result replaces an earlier private diagnostic output that changed only certificate bytes.
That invalid output is discarded and supplies no evidence.

This fixture ran every component on one Docker host.
The worker projects simulate separate hosts without district network or host failure.
The tmpfs volume does not qualify district RWX storage, quotas, NFS, or object storage.
Synthetic certificates do not qualify district certificate operations.
The result sets `qualifiesProfile` and `acceptedRelease` to `false`.

## Kubernetes process and shared-artifact run

[The Kubernetes result](CC-18-kubernetes-result.json) records six faults against the restored synthetic namespace.
It covers API, workers, Redis, application PostgreSQL, Kestra, and shared artifact access.
Every fault prevented full startup readiness.
Worker, Redis, Kestra, and artifact faults produced named failures through the protected public route.
Application PostgreSQL loss denied public bootstrap authentication.
The verified internal API diagnostic named `application-database` while the API process remained available.
API loss left the public route unavailable until both API replicas recovered.

The first worker fault exposed unavailable diagnostics despite healthy API and edge processes.
Kubernetes had removed those dependency-failed pods from their Services.
The renderer now uses process liveness for API and edge pod readiness.
Protected startup remains the eight-component installation readiness gate.
Worker and stateful probes retain their dependency checks.

All six faults recovered within 2,035 to 22,486 milliseconds.
Every recovery preserved the 8 MiB artifact checksum, Kestra flow, and Kestra marker checksum.
The harness restored every replica count and artifact mode.
The result contains redacted service logs, pod placements, restart counts, and configured resource bounds.

This run used Kubernetes 1.35.8 on the dedicated three-node Kind fixture.
Kind hostPath storage shares one Docker host.
The run does not qualify district RWX storage, an enforcing CNI, or district LoadBalancer behavior.
Local immutable images came from the uncommitted worktree.

## Kubernetes edge certificate faults

[The Kubernetes certificate result](CC-18-kubernetes-certificate-result.json) records two faults against the complete isolated Kubernetes fixture.
The fixture established eight ready checks before changing the edge certificate.
It used a matching private key for each injected certificate.

The expired certificate produced `CERT_HAS_EXPIRED`.
The wrong-host certificate produced `ERR_TLS_CERT_ALTNAME_INVALID`.
Each recovery restored eight ready checks.
Final cleanup restored the original CA, certificate, and private-key bytes.

One baseline validation and four later comparisons preserved the ready artifact and the Kestra fixture.
The artifact retained its 54-byte size, publication state, and SHA-256.
The Kestra fixture retained revision 1 and its marker SHA-256.

The 64,998-millisecond run used Kubernetes 1.35.8 on a three-node Kind fixture.
It covered the Kubernetes edge certificate cell only.
The fixture ran on one Docker host.

CC-18 now lacks only the bounded Kubernetes capacity cell.
The existing Kind artifact loss result does not supply that capacity evidence.
