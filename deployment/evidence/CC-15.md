# CC-15 Kubernetes profile

## Current acceptance result

The synthetic Kubernetes acceptance criteria passed on 2026-09-09.
The published candidate ran on three Kubernetes 1.35.8 nodes with Calico 3.32.2.
All eight Deployments remained ready during the network-policy check.

The [enforcement result](CC-15-network-policy-result.json) records 29 connection checks.
Six DNS checks and eleven declared service paths passed.
Twelve prohibited paths timed out under the enforced ingress and egress policies.
Trusted checks used existing workload identities. The untrusted probe did not enter ready service endpoints.

The earlier workload results supply replica, rescheduling, storage, migration, and protected startup evidence.
Together, these results meet the ticket's synthetic criteria.
They do not qualify a district CSI driver, LoadBalancer, or independent physical failure domains.

## Historical implementation record

Date: 2026-09-08. Renderer: `deployment/kubernetes/cli.mjs` and `render.mjs`.
The all-local synthetic fixture renders 39 Kubernetes resources.
Six semantic tests and deployment lint passed through Nx.

| Acceptance criterion                                         | Available evidence                                                                                                 | Remaining gate                                  |
| ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------- |
| Shared pinned images and declared dependencies               | Both synthetic namespaces ran the three recorded local image digests and all pinned upstream images.               | Published release retrieval from district nodes |
| Ingress, discovery, secrets, storage, probes, and migrations | Eight workloads passed startup checks through protected HTTPS in the source and restored target namespaces.        | District CSI and LoadBalancer validation        |
| Internal access restrictions                                 | Default deny, DNS, component paths, and external CIDR policies passed API validation.                              | Enforcing district CNI tests                    |
| Multiple API and worker instances                            | Two API replicas and two worker replicas reached readiness across the three Kind nodes.                            | District capacity validation                    |
| Worker reschedule and artifact read                          | A replacement worker on another node read the existing 8 MiB artifact with its recorded checksum.                  | District RWX reschedule validation              |
| Application and Kestra storage                               | Operations restored both databases, artifacts, Kestra internal storage, configuration, and release into new roots. | District storage recovery                       |
| Prerequisites and limitations                                | Evidence records namespaces, PVC identities, writer quiescence, local adapter boundaries, and remaining gates.     | District installation walkthrough               |

The parent task created a disposable Kind 0.33.0 cluster with Kubernetes 1.35.8.
Node image: `kindest/node:v1.35.8@sha256:07b2536e30b803ed61d1677a79df6115f798ce64c80f9e22f6ed45afd09323c0`.
The fixture has one control-plane node and two worker nodes.
The validation used its explicit kubeconfig and context.
The dry-run namespace was `cc-kube-render-validation`.

Command:

```sh
kubectl --kubeconfig /tmp/cc-phase1-kubeconfig --context kind-cc-phase1-validation apply --dry-run=server -f /tmp/cc-kubernetes-rendered.json
```

All 39 resources passed server-side validation.
The command did not deploy application workloads or create Secrets.
The rendered application image references were synthetic fixtures.
Only the dedicated validation namespace was created for this check.

The Kind CNI does not establish NetworkPolicy enforcement.
The fixture's local-path storage does not establish shared-filesystem or district CSI behavior.
No production cluster received changes.
No district LoadBalancer or external endpoint was tested.
The later hosted and enforcing-CNI results supersede this initial runtime acceptance gap.

## Executed workload fixture

A dedicated three-node cluster ran the recorded application images and TLS services.
All eight Deployments reached their requested ready replica counts.
Two API replicas and two worker replicas ran concurrently.
The runtime check exposed a missing Kestra worker endpoint variable.
The renderer now supplies `ENV_CC_WORKER_BASE_URL` from the deployment configuration.
Fault qualification exposed dependency-aware API and edge pod probes that removed diagnostic endpoints.
Their Kubernetes readiness probes now use process liveness.
Protected startup continues to report installation readiness across all eight components.

Workers on separate Kubernetes nodes published and read an 8 MiB artifact with matching SHA-256 checksums.
The test cordoned the writer node and deleted the writer pod.
Its replacement ran on another node and read the same artifact successfully.
[Machine evidence](CC-15-workload-result.json) records pod names, node names, and checksums.

The restore run stopped API, workers, and Kestra before backup.
The actual operations module backed up both PostgreSQL databases and both file trees.
It restored them into `cc-kube-restore-synthetic` with new PVC identities and file roots.
The restored namespace reached all eight ready checks.
Its worker read the restored artifact, and Kestra exposed the restored flow and marker.
The target Redis did not contain the source marker.
[Restore evidence](CC-17-kubernetes-result.json) records the exact namespaces, paths, images, and table inventories.

This fixture used hostPath volumes over one shared host directory.
The test untainted the disposable control-plane node to provide spare scheduling capacity.
These conditions establish renderer and application behavior in the synthetic fixture.
They do not establish district RWX durability, CSI recovery, NetworkPolicy enforcement, or LoadBalancer behavior.
The evidence records uncommitted source and does not qualify a signed release.
