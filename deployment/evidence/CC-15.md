# CC-15 Kubernetes profile

Date: 2026-09-08. Renderer: `deployment/kubernetes/cli.mjs` and `render.mjs`.
The all-local synthetic fixture renders 39 Kubernetes resources.
Six semantic tests and deployment lint passed through Nx.

| Acceptance criterion                                         | Available evidence                                                                                         | Remaining gate                                                 |
| ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| Shared pinned images and declared dependencies               | Renderer checks release inventory equality and omits external-owned workloads.                             | District image retrieval and complete installation             |
| Ingress, discovery, secrets, storage, probes, and migrations | Deterministic List includes L4 HTTPS ingress, scoped Secret references, PVCs, probes, and preparation Job. | Runtime listener, credential, CSI, and LoadBalancer validation |
| Internal access restrictions                                 | Default deny, DNS, exact component paths, and external CIDR policies pass API validation.                  | Enforcing district CNI tests                                   |
| Multiple API and worker instances                            | Renderer requires at least two replicas and required worker node anti-affinity.                            | Running infrastructure probes across nodes                     |
| Worker reschedule and artifact read                          | Shared PVC mounts and repeatable cross-node procedure supplied.                                            | Actual cross-node read after reschedule                        |
| Application and Kestra storage                               | Separate RWO databases and separate RWX artifact/internal-storage PVCs rendered.                           | District filesystem availability and recovery                  |
| Prerequisites and limitations                                | Operator guide records ownership, spare-node capacity, edition restrictions, and verification commands.    | District walkthrough and observed recovery record              |

The parent task created a disposable Kind 0.33.0 cluster with Kubernetes 1.35.8.
Node image: `kindest/node:v1.35.8@sha256:07b2536e30b803ed61d1677a79df6115f798ce64c80f9e22f6ed45afd09323c0`.
The fixture has one control-plane node and two worker nodes.
The validation used its explicit kubeconfig and context with the dedicated `cc-kube-render-validation` namespace.

Command:

```sh
kubectl --kubeconfig /tmp/cc-phase1-kubeconfig --context kind-cc-phase1-validation apply --dry-run=server -f /tmp/cc-kubernetes-rendered.json
```

All 39 resources passed server-side validation.
The command did not deploy application workloads or create Secrets.
The rendered application image references were synthetic fixtures.
Only the dedicated validation namespace was created for this check.

The Kind CNI does not establish NetworkPolicy enforcement.
The fixture's local-path storage does not establish shared-filesystem or district CSI behavior.
No production cluster received changes.
No district LoadBalancer or external endpoint was tested.
CC-15 remains incomplete until those runtime acceptance gates pass.

## Executed workload fixture

A second disposable cluster ran the real application images and TLS services.
All eight Deployments reached their requested ready replica counts.
Two API replicas and two worker replicas ran concurrently.
The runtime check exposed a missing worker dispatch credential environment variable. The renderer now supplies that variable.

Workers on separate Kubernetes nodes published and read an 8 MiB artifact with matching SHA-256 checksums.
The test cordoned the writer node and deleted the writer pod.
Its replacement ran on another node and read the same artifact successfully.
[Machine evidence](CC-15-workload-result.json) records pod names, node names, and checksums.

This fixture used hostPath volumes over one shared host directory.
The test untainted the disposable control-plane node to provide spare scheduling capacity.
These conditions establish renderer and application behavior in the laboratory.
They do not establish district RWX durability, CSI recovery, enforcing NetworkPolicy, or LoadBalancer behavior.
The evidence records uncommitted source and does not qualify a signed release.
