# Application contracts

This library defines shared runtime schemas and TypeScript types for Phase 2.
The API and frontend use these contracts for identity, sessions, preferences, diagnostics, and errors.
Contracts have no Angular, NestJS, database, cache, or provider dependencies.
